app.name=keywords

logging.file=./logs/${app.name}-test.log

## can't specify any profile stuff here because can't be overridden on comdline. 
# spring.profiles=dev,test,prod,mysql
# spring.profiles.include=h2,mysql

## can't specify any profile stuff here because can't be overridden on comdline. 
# spring.profiles.active=dev,mysql

# spring.jpa.database: H2
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# datasource.jobkeywords.url=jdbc:h2:mem:AZ;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
datasource.jobkeywords.url=jdbc:h2:mem:keywordtestdb;MODE=MYSQL;DB_CLOSE_ON_EXIT=FALSE
datasource.jobkeywords.driverClassName=org.h2.Driver
datasource.jobkeywords.username=
datasource.jobkeywords.password=
# datasource.jobkeywords.poolSize=30

## spring.jpa.hibernate.ddl-auto = update
## spring.jpa.hibernate.ddl-auto = create-drop
## spring.jpa.hibernate.naming_strategy = org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.hibernate.show_sql = true
spring.jpa.hibernate.format_sql = true

